{
  "name": "aws-career-sandbox",
  "private": true,
  "version": "0.1.0",
  "description": "Bounded, zero-running-task AWS career sandbox",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "cdk": "cdk",
    "synth": "cdk synth --strict",
    "validate": "npm run typecheck && npm test && npm run synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.266.0",
    "cdk-nag": "^2.37.55",
    "constructs": "^10.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "aws-cdk": "2.1138.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
